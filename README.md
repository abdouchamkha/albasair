# منصة البصائر التعليمية - Vue.js Application

تطبيق Vue.js تفاعلي لمنصة البصائر التعليمية مع خريطة تعلم على طراز Duolingo.

## المميزات

### 🗺️ خريطة التعلم التفاعلية
- تصميم مستوحى من Duolingo مع مسار تعليمي مرئي
- مستويات مقفولة ومفتوحة حسب التقدم
- نظام النجوم والنقاط
- تتبع التقدم الشخصي

### 📚 الدروس التفاعلية
- ألعاب تعليمية (محاكاة الوضوء والصلاة)
- قصص تفاعلية مع خيارات متعددة
- دروس صوتية (أذكار يومية)
- مهام فيديو وقوائم مهام
- اختبارات تفاعلية مع تغذية راجعة فورية

### 🎮 نظام الألعاب
- نقاط خبرة (XP)
- نظام النجوم (1-3 نجوم لكل مستوى)
- تتبع التقدم والإنجازات
- فتح المستويات التدريجي

## هيكل المشروع

```
vue-app/
├── src/
│   ├── components/          # مكونات Vue
│   ├── views/              # الصفحات الرئيسية
│   │   ├── Home.vue        # الصفحة الرئيسية
│   │   ├── LearningMap.vue # خريطة التعلم
│   │   └── Lesson.vue      # صفحة الدرس
│   ├── router/             # إعدادات التوجيه
│   ├── stores/             # حالة التطبيق (Pinia)
│   ├── types/              # تعريفات TypeScript
│   ├── data/               # البيانات الثابتة
│   │   └── curriculum.ts   # بيانات المنهج التعليمي
│   └── assets/             # الملفات الثابتة
├── public/                 # الملفات العامة
└── package.json           # إعدادات المشروع
```

## التقنيات المستخدمة

- **Vue 3** مع Composition API
- **TypeScript** للبرمجة الآمنة
- **Vue Router** للتوجيه بين الصفحات
- **Pinia** لإدارة الحالة
- **Vite** كأداة بناء سريعة
- **CSS** مع متغيرات CSS للتصميم

## تركيب وتشغيل المشروع

### المتطلبات
- Node.js 16 أو أحدث
- npm أو yarn

### الخطوات

1. **نسخ المشروع**
   ```bash
   cd /home/dz/Albsair/vue-app
   ```

2. **تثبيت الاعتماديات**
   ```bash
   npm install
   ```

3. **تشغيل خادم التطوير**
   ```bash
   npm run dev
   ```

4. **فتح التطبيق**
   افتح المتصفح على العنوان الذي يظهر في الطرفية (عادةً `http://localhost:5173`)

## البيانات

يستخدم التطبيق بيانات المنهج التعليمي من ملف `curriculum.json` الذي تم تحويله إلى TypeScript في `src/data/curriculum.ts`. يتضمن:

- **المراحل التعليمية** (التأسيس، البناء، إلخ)
- **المستويات** مع أنواع مختلفة من المحتوى
- **الاختبارات** والأسئلة التفاعلية
- **الأعمدة التعليمية** (العبادة، المسؤولية، التعاطف، إلخ)

## المكونات الرئيسية

### Home.vue
- الصفحة الرئيسية مع تصميم عصري
- أقسام المنصة التعليمية
- زر الدخول التجريبي السريع

### LearningMap.vue
- خريطة التعلم التفاعلية
- عرض المستويات كعقد متصلة
- حالة المستوى (مكتمل، حالي، مقفول)
- نافذة منبثقة لتفاصيل المستوى

### Lesson.vue
- صفحة الدرس الديناميكية
- دعم أنواع مختلفة من الدروس
- اختبارات تفاعلية
- تتبع التقدم داخل الدرس

## التخصيص

### إضافة مستوى جديد
1. أضف البيانات في `src/data/curriculum.ts`
2. سيتم عرض المستوى تلقائياً في خريطة التعلم

### تعديل الألوان
عدل المتغيرات في `src/assets/styles.css`:
```css
:root {
  --primary: #4361ee;
  --secondary: #3a0ca3;
  --accent: #7209b7;
  /* ... */
}
```

### إضافة نوع درس جديد
1. أضف النوع في `src/types/curriculum.ts`
2. أضف القالب المقابل في `Lesson.vue`

## النشر

لإنشاء نسخة إنتاجية:
```bash
npm run build
```

سيتم إنشاء الملفات في مجلد `dist/`.

## الملاحظات

- التطبيق يستخدم localStorage لحفظ تقدم المستخدم
- التصميم متجاوب ويعمل على جميع الأجهزة
- يدعم اللغة العربية RTL بالكامل
- يمكن توسيعه بسهولة لإضافة المزيد من الميزات
